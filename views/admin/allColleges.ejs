<%- include('./header.ejs') %>

<!-- partial -->
<div class="main-panel">
  <div class="content-wrapper">
    <!-- notification message -->
    <% if(message){ %>
    <div
      class="alert alert-dismissible fade show alert-<%= message.type %>"
      role="alert"
    >
      <button
        class="btn-close"
        type="button"
        data-bs-dismiss="alert"
        aria-label="close"
      ></button>
      <strong><%= message.message %></strong>
    </div>
    <% } %>
    <!--  -->
    <div class="card">
      <div class="card-body">
        <h4 class="card-title">Data table</h4>
        <div class="row">
          <div class="col-12">
            <div class="table-responsive">
              <table id="order-listing" class="table">
                <thead>
                  <tr>
                    <th>S.NO</th>
                    <th>Logo</th>
                    <th>College Name</th>
                    <th>Address</th>
                    <th>City</th>
                    <th>State</th>
                    <th>Pin Code</th>
                    <th>Actions</th>
                  </tr>
                </thead>
                <tbody>
                  <% colleges.forEach(function(college, index) { %>
                  <tr>
                    <td><%= index+1 %></td>
                    <td><img src="<%= college.clgLogo %>" alt="logo" /></td>
                    <td><%= college.name %></td>
                    <td><%= college.address %></td>
                    <td><%= college.city %></td>
                    <td><%= college.state %></td>
                    <td><%= college.pinCode %></td>
                    <td>
                      <a
                        href="/updateCollege/<%= college._id %>"
                        class="btn btn-outline-primary"
                        >Edit</a
                      >
                      <form
                        class="delete-form"
                        action="/colleges/<%= college._id %>?_method=DELETE"
                        method="POST"
                      >
                        <button
                          class="btn btn-outline-danger"
                          type="submit"
                          onclick="return confirm('Are you sure you want to delete this college?')"
                        >
                          Delete
                        </button>
                      </form>
                    </td>
                  </tr>
                  <% }); %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('./footer.ejs') %>
